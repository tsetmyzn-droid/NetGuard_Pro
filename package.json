{
  "name": "netguard-pro",
  "version": "5.0.0",
  "description": "Native Flutter Router Manager",
  "main": "server.ts",
  "scripts": {
    "build": "mkdir -p dist && echo '<h1>NetGuard Pro Native Engine</h1><p>Environment: Production Blueprint v5.0.0</p><p>Status: Native Flutter Code Active in /lib</p>' > dist/index.html && rm -rf OpenWrtManager-1.40 luci-mobile-1.1.8 .isolated-env archive",
    "start": "node server.ts",
    "dev": "vite",
    "lint": "echo 'Linting native Dart code...'"
  },
  "dependencies": {
    "path": "^0.12.7",
    "uuid": "^14.0.0"
  }
}
